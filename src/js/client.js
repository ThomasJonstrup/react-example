import React from "react";
import ReactDOM from "react-dom" // for at manipulerer med dom

import Layout from "./components/Layout";

const app = document.getElementById('app');
ReactDOM.render(<Layout/>, app); // Render Layout
